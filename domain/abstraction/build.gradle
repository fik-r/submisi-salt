plugins {
    alias(libs.plugins.android.library)
    alias(libs.plugins.kotlin.android)
}

android {
    namespace 'com.example.abstraction'
    compileSdk 33
}

dependencies {
    implementation project(':domain:model')
    implementation project(':common')

    implementation(libs.coroutines)
}